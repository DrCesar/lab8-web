
const addNumber = number => ({
  type: 'ADD_NUMBER',
  number,
});

// const addOperand = operand => ({
//   type: 'ADD_OPERAND',
//   operand,
// });

const actions = {
  addNumber,
};

export default actions;
